import { Box } from '@mui/material'
import BorderBox from '../../layout/BorderBox'

type Props = {}

const Profile = (props: Props) => {
  return <BorderBox>Profile</BorderBox>
}

export default Profile
