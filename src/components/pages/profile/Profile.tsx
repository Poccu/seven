import { Box } from '@mui/material'
import BorderBox from '../../ui/BorderBox'

type Props = {}

const Profile = (props: Props) => {
  return (
    <BorderBox>
      <Box sx={{ p: 3 }}>Profile</Box>
    </BorderBox>
  )
}

export default Profile
